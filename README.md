# homebrew-dico

```sh
brew install --cask mechanicpanic/dico/dico
brew upgrade --cask dico
```

Installs **Dico.app** (the popup, ⌥D) and the `dico` command. The app is signed ad-hoc: open it once via System Settings ▸ Privacy & Security ▸ Open Anyway.
